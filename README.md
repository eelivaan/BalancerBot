A two-wheeled robot that uses PID-controller with accelerometer and gyroscope data to stay in balance. 
The project also features a desktop user interface for tuning the PID parameters wirelessly.

## UI dependencies

```bash
pip install bleak numpy matplotlib
```
